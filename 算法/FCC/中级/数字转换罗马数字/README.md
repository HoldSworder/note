```js
function convert(num) {
  
    var a = num.toString();
    var b = [];
  
    for(var i = 0; i < a.length; i++) {
      b.push(a[i]);
    }
    
    for(var l = 0; l < b.length; l++) {
      
    }  
  
    return b;
}

convert(36);


```